services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: moneykeeper
    ports:
      - "5432:5432"
    volumes:
      - moneykeeper_pg:/var/lib/postgresql/data

volumes:
  moneykeeper_pg:
